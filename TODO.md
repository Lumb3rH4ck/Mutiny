# Mutiny TODO

## TUI
- [x] **Center the title** — move the "Mutiny" wordmark and its icon in the Title (header) to the center of the screen (rendered as `⚓ Mutiny ⚓`, nudged 9 cols right of true center).
- [x] **Per-step scan labels** — label every step of the scanning process as `ClamAV Scan: 1 of x` / `Yara Check: 1 of X` as each file is checked. Scanner emits a `ScanStep` before each engine pass (`internal/scanner/scanner.go`, `ScanFileStep` + `Progress` hook); `main.go` broadcasts `scan.step` `{torrent_id, file, engine, index, total, label}` on the hub and logs each step.
- [x] **Inline scan progress** — when viewing a download currently `scanning`, show the current step next to the download's title, flanked by two sword icons (⚔). Manager tracks the live step per torrent (`SetScanStep`) → `TorrentInfo.ScanStep` surfaced in `List()`/`Get()`/API; `renderTorrentCard` shows `⚔ <step> ⚔` (amber) after the name; cleared when the torrent's scan completes.
- [x] **Verbose scan popup (`V`)** — press `V` to open a small popup window with verbose details of ongoing scans (per-file engine results, progress, quarantine actions). Shows the live step (`⚔ … ⚔`), aggregated verdict, quarantine destination, and per-file `clean`/`threat: <sig>`/`unscanned`/`pending` rows (arrow keys scroll long file lists, Esc closes).
- [x] **Delivered message** — once a download's scan completes (delivered to `clean/`), change the text to the right of the download title to `{~~ Aarrr, the goods be delivered! ~~} ` (replaces the `⚔ <step> ⚔` that shows while scanning).
- [x] **Per-step scan info on previous downloads** — surface per-step scan details (scan name: ClamAV / YARA, and pass/fail for each) in the scan info shown on the history / previous-downloads page. Requires persisting each engine's per-file outcome (currently only the aggregated `clean`/`threat`/`unscanned` verdict is recorded, no per-engine breakdown).
- [x] **Downloads-page scan keys declutter** — on the Downloads page the scan-related keys exist: `s` opens the Speed Cap popup, `v`/`V` opens the Scan Details popup, `r` re-scans the highlighted download. Keep re-scan only on the **Previous Downloads** page (where delivered items live) and remove the scan actions on the Downloads page (unbind `s` and `v`/`V` there and drop them from the Downloads footer help — `mainHelp`/`mainHelpNarrow` in `tui.go`; `R re-scan` stays in the history help `histHelp`).
  - Done in `tui.go`: `s` / `v` / `V` unbound on the Downloads page, `r` now only re-scans from the Previous Downloads page, `mainHelp`/`mainHelpNarrow` drop `Speed Cap`/`Scan Details`/`Re-Scan` and gain `T - Settings`; the old `renderSpeedPopup` / `renderScanPopup` / `statusSuffix` / `rescanDownloadsSel` helpers were deleted and their tests rewritten.

## Scan integrity
- [x] **Clean requires both engines to agree** — a file is `clean` only when BOTH ClamAV and YARA report clean; any engine-down → `unscanned` (torrents strand in `scanning/` if only one engine runs — by design).
- [x] **Archive recursion for YARA** — modern YARA has no native archive recursion (`-l` = `--max-rules`, `-r` = dir recursion only), so archives (.zip/.tar/.gz/.7z/.rar/…) are first extracted on the host via `bsdtar` into a throwaway dir under the download root (chmod-normalized so the container can read it), then `yara -r -w` scans the extracted tree — in addition to the raw archive. Extraction failure degrades to a raw-file scan.

## Sandbox / reputation (in suggested order)
- [x] **Hash reputation lookups** — check SHA-256 against MalwareBazaar / VirusTotal before clean delivery (hashes only, no file contents sent).
- [x] **Containerized scanning** — ClamAV (`clamscan`) + YARA run inside a long-lived Docker container with the download root and rules dir bind-mounted read-only; release to `clean/` only on clean verdict.
- [x] **Automated behavior analysis** — throwaway container sandbox (network cut, capabilities dropped, strace/auditd/Falco capture of file/net/exec ops) for executable-ish file types only. High effort.

## Operations / maintenance
- [x] **Weekly engine + rule auto-update** — once a week, refresh both scanners: YARA rules (`~/.local/share/mutiny/rules`, e.g. git pull) and the ClamAV engine + signatures (`clamav` + `freshclam` in the scan container). Should run on a timer without disrupting scans (rule-set swap is already safe — the scanner revalidates/recompiles only standalone rules).

## Settings & themes
- [x] **In-app settings with theme selection** — add a settings page/dialog in the TUI (and ideally the web UI) where the user can pick the active theme and toggle other user-facing settings live, instead of only editing `config.yaml`. Theme is already wired from config (`cfg.Theme` → `themeFromName`, `config.yaml: theme`); a TUI `Settings` popup should cycle/select themes and save the choice back to the config file (or a `~/.config/mutiny` override) so it survives restart. Other toggleable settings to consider exposing: panic_enabled, scan_on_the_fly, scan_on_completion, hash_reputation, max_download_rate / max_upload_rate, auto_update / update cadence, dht_enabled, and file-selection default (wait vs auto-download-all on add).
  - Done in `settings.go`: TUI `Settings` popup (`T`/`t` on the Downloads page) cycles all eleven managed settings — `theme`, `max_download_rate`, `max_upload_rate`, `panic_enabled`, `scan_on_the_fly`, `scan_on_completion`, `hash_reputation`, `auto_update`, `update_interval`, `dht_enabled`, `wait_for_selection` — via `enter`/`space`/`l`/`r` (with `↑↓` navigation/scroll and `esc` to close). Each change persists to `config.yaml` (`saveConfigSetting`, fallback `~/.config/mutiny/config.yaml`) and applies live where possible: theme re-paints immediately (`paintBackground`), rates push `tm.SetGlobalRateLimit`, `panic_enabled` pushes `vpnMon.SetPanicEnabled`; restart-flagged rows (`hash_reputation`, `auto_update`, `update_interval`, `dht_enabled`) are tagged `(restart)`. Adds also honor the new `wait_for_selection` config key via a shared `addAny` route (picker vs auto-download-all).
  - Web UI done too: the browser exposes the same setting sections via `GET /api/settings` and live-cycles values through `POST /api/settings/cycle` (shares the TUI's `cycleSettingByKey` in `settings.go`), and re-themes the page instantly via `body[data-theme]`.
- [x] **Generate extra themes** — added `Theme` bundles alongside `pirateTheme()` (registered in `themeFromName`, `tui.go` and listed in the settings picker): `cherry-blossom` (off-white/pink, flower icons), `neon` (translucent purple, electric accents), `/home` (deep gray, white terminal text, computer icons) and `coffee` (tan/cappuccino, café icons). `pirate` is now the default (the old `default` theme was removed). Layout and the title bar stay identical across themes; each theme now carries its own `IconSet` (progress-bar ship/island/globe, history/report markers, scan fences, peer/pick glyphs) so only colours + glyphs change per look.

## OS integration
- [x] **Default handler for `.torrent` + `magnet:`** — make mutiny the system default so a magnet link clicked in a browser and a `.torrent` double-clicked in a file manager auto-add to mutiny.
  - CLI: accept positional args on launch — `mutiny -mode tui [file.torrent] [magnet:...]` — and auto-add them from the TUI's `Init()` (`initialAdd` → `addAsync` per arg).
  - Desktop entries: `build/mutiny.desktop` and `build/mutiny-tui.desktop` now carry `MimeType=application/x-bittorrent;x-scheme-handler/magnet;` and an `%U` exec slot; the Makefile install target registers the default via `xdg-mime default mutiny.desktop application/x-bittorrent`, `xdg-mime default mutiny.desktop x-scheme-handler/magnet`, and `xdg-settings set default-url-scheme-handler magnet mutiny.desktop`.
  - Single-instance handoff: `main()` probes `GET /api/status` on 127.0.0.1:3030 BEFORE touching the state store, torrent manager, or torrent port; if an instance is listening it POSTs each `.torrent` (multipart) / magnet (JSON) to `/api/torrents` with `api_token` Bearer auth (from config), logs the handoff, and exits 0. TUI mode now also runs the API server in the background so a running TUI session is handoff-able. No listener → the new process starts its own session and directly adds the args.
  - Test matrix: Chrome/Firefox magnet click, Thunar/Nautilus double-click, command-line `xdg-open`, and the already-running case (handoff verified end-to-end: magnet lands in the live session, `.torrent` reaches the API, second process exits 0).

## Add sources
- [x] **HTTP(S) direct links in the add flow** — let the "add torrent" prompt, the API, and the web UI accept `http://` / `https://` URLs pointing at a downloadable file (typically a `.torrent`, but any direct link) so the download gets fetched and then scanned like any other source. Done: TUI `a`-prompt detects `http(s)` URLs (optional referrer prompt), `POST /api/torrents` accepts `{"url": ...}` / `{"referrer": ...}`, CLI args / single-instance handoff forward URLs, browser-UA + referrer handling applied at fetch time, and the entry runs the same scan → deliver → Previous Downloads pipeline.
  - URL detection: in the TUI input parse treat `http://`/`https://` as a fetch-able source instead of defaulting to magnet; a new `addURL`/`AddTorrentViaURL` path downloads the payload into a temp file and feeds it through `metainfo.LoadFromFile` → `AddTorrentFile` so it lands in `scanning/` and gets ClamAV/YARA-scanned on completion.
  - API + web: `POST /api/torrents` gains a `{"url": "https://…"}` branch in `internal/api/server.go` `addTorrent`; `web/app.js` submits its add input as-is; single-instance handoff (`main.go` `postTorrent`/`postMagnet`) forwards a URL arg so an already-running session picks it up.
  - Guardrails: enforce `http`/`https` scheme, a sane size limit on the fetch, and a timeout; on download failure surface the error in the TUI/API instead of silently defaulting to magnet.

## Seeding (re-seed delivered downloads)
- [x] **Toggle-able re-seeding of delivered torrents** — a finished download can go back to seeding into the swarm. Two controls share a dedicated seed client (`internal/torrent/seed.go`, `SeedEngine`, second anacrolix client with `Seed=true`, `DataDir=<clean>/`, listen port `TorrentPort+1`, VPN bind replicated): a global **Re-Seed Completed Downloads** toggle in Settings → DOWNLOAD OPTIONS (`seed_completed`, default off) and a per-item **seed/un-seed** toggle per clean history entry. Only "clean" entries that still carry their bencoded metainfo are seedable (URL downloads and pre-feature delivered entries with no saved metainfo can't be re-seeded — documented). State store persists `Metainfo []byte` + `Seeding bool` per entry (`internal/state/store.go`); metainfo is captured at delivery (`tm.Metainfo(id)`, anacrolix `Torrent.Metainfo()`) and preserved through re-scans. The manager exposes `Metainfo(id) ([]byte, error)`. The seed controller (`main.go`: `seedController`, created as `seedSvc`) lazily spins the engine, tracks per-item marks in the live state, and exposes `SeedNow/UnseedOne/Seeding/Toggle/SetEnabled/UploadRate/ReseedAll/UnseedAll`. Wiring: `completeTorrent` captures metainfo + sets `Seeding:true` and auto-seeds clean deliveries when the global toggle is on; `rescanTorrent` preserves both fields and applies the verdict (clean→reseed if marked, threat/unscanned→unseed + clear mark); startup `ReseedAll` after the re-park loop; VPN panic `g` and `vpnMon` panic/recover hooks drain/reseed the seed client. UI: TUI `s` key on the Previous Downloads page (`toggleSeedSel`), a `⬆ seeding` badge on clean seeding rows, `s seed/un-seed` in the help footer, unseed-before-delete in `deleteHistorySel`; web `s` key + `toggleSeed()` (`POST /api/history/{infohash}/seed`) + `seeding` badge + footer hint. API: `WebHistoryEntry` + `Seedable`/`Seeding` fields, `"seed"` action in `handleWebHistoryByID`. Settings: new `seed_completed` row, `cycleSettingByKey` case calls `sctl.SetEnabled`, `cycleRate` calls `sctl.UploadRate`. Tests: `internal/torrent/seed_test.go` (single/multi-file active, missing-data rejection, drain-all), `TestSettingsSeedCompletedCycle`, `TestLoadConfigSeedCompletedMerge`. Gate: `skylos . --gate` PASSES (quality budget ≤131, deps 43, dead code 0, secrets 0).

## Web UI (v2 — TUI parity)
- [x] **Web UI redesign to full TUI parity** — `web/app.js` Alpine state machine (torrents/history/vpn/engines/panic/settings/picker/rescan), page-aware keyboard map mirroring the TUI keys, WS + polling rendering; `web/index.html` Downloads/Previous Downloads/settings/picker/panic overlays; `web/style.css` five theme palettes via `body[data-theme]` CSS vars; `.c-progress/.c-clean/.c-threat/.c-unscanned/.c-label` color utilities; `{~~ Aarrr, the goods be delivered! ~~}` + ship-size icons matched to the TUI.
- [x] **Web parity backend** — `/api/settings` + `/api/settings/cycle` (shared `cycleSettingByKey`), `/api/history` + `/api/history/{id}/rescan|delete|open` (reuses `rescanTorrent`; `ID = sha256(dir)[:8]`), `/api/engines`; `WebServices` struct of funcs (nil = 404) in `internal/api/web.go` + `web_services.go`.
- [x] **Cookie-based API auth** — `mutiny_token` HttpOnly SameSite=Strict cookie seeded on GET when `api_token` set; `tokenMatches` accepts cookie; browser POSTs work sans Bearer header.
- [x] **LAN + Tailscale serving** — `web_lan` / `web_tailscale` (default true; `-no-web-lan` / `-no-web-tailscale` overrides, TUI NETWORK OPTIONS rows) enumerate real interfaces in `listenAddrs`; keep torrent sockets VPN-pinned.
- [x] **Compact header** — statuses (VPN + ClamAV + YARA + Sandbox) inlined into one row beside the title (was 4 stacked rows); tighter `screen`/`menubar` spacing.
- [x] **Per-file download selection when adding** — before a torrent's data starts downloading, let the user choose which files inside it they actually want (skip e.g. sample/readme directories). Done: manager `SelectFiles`/`SelectAll`/`WithFiles`/`WaitForSelection` (`applyFiles` sets per-file anacrolix priorities, `selectionComplete`/`IsComplete` make completion/scanning subset-aware), state store persists the chosen set, API `/select` + add-body `files`/`wait`, TUI picker popup (auto-opens on awaiting adds, `space` toggle / `j` `k` / `l` `u` / `enter` subset / `esc` all / `f` reopen), web picker modal + "choose files" checkbox. Every add path waits for a choice — `a`-prompt, CLI args/OS handoff (`initialAdd` + `postMagnet`/`postTorrentFile` send `wait: true`) — with restore being the only silent resumer.
  - Manager: replace the hard-coded `DownloadAll()` with a select-then-download step — a method like `SelectFiles(id, paths []string)` (or `SetDownloadPaths`) that maps the chosen files to their pieces via anacrolix `t.Files()[i].Priority()` (default all if nothing picked). For `.torrent` adds (metadata known up front) this can gate the download start; for magnets it must wait for `GotInfo()` then present the picker once metadata arrives.
  - TUI: when an add completes, open a file-selection prompt (reuse the browse-style list: arrow keys + space to toggle, path + size shown) that only appears before download begins; `t.DownloadAll()` should not fire until the user confirms.
  - API + web: `POST /api/torrents` accepts an optional `"files": ["Sub/a.bin", …]` on the magnet/URL body (and multipart upload); `web/app.js` offers the same picker post-add. Omitted → all files.
  - Restore/compat: persist the chosen file set (state store) so a restarted session re-applies it instead of falling back to full download; already-in-progress torrents without a selection keep current behavior.
  - Implementation notes for future maintenance: file paths in selection are relative to the torrent root and are the same identifiers the API/TUI show (`f.DisplayPath()`, which for multi-file torrents is the path *without* the torrent-name prefix). `t.DownloadAll()` only schedules pieces and leaves file priority at `None` — the manager's full-download path calls `downloadAll` instead so per-file priority stays uniform with subsets. Selection drives completion via `selectionComplete` because anacrolix `Complete()` is true only when *every* piece exists.

## Static analysis (Skylos 4.36.1 scan — 2026-09-13)
First run: `skylos . -a` → 42 files, 3 flagged dead functions, 1 AI defect, 4 security, 144 quality, exit 2 (Go engine unavailable). Triaged against source below.

NOTE 2026-09-13 (after this): `skylos[all]` is only resolvable on Python 3.14 at **4.23.0** (the `[all]`/`[llm]` extra pins `litellm>=1.84,<1.85` which requires Python<3.14), so this venv fell back from 4.36.1 → 4.23.0 (with all extras). The Go engine (`skylos-go`) is NOT in the wheel — it's built from the repo: `go build -o skylos-go ./cmd/skylos-go` in `skylos/engines/go` (matched to tag matching the pip version). Built from tag `v4.23.0`, installed at `~/.local/bin/skylos-go` (PATH discovery). Engine confirmed working: `skylos . -a --json` now completes (note: use `--json` for repeat scans — the plain console formatter hung at 5min+).

### Environment / tooling (do first)
- [x] **Install the Skylos Go engine** (`skylos-go`) — DONE: built from repo tag `v4.23.0` (`skylos/engines/go`, `go build -o skylos-go ./cmd/skylos-go`), installed at `~/.local/bin/skylos-go`; doctor shows it available, `skylos . -a --json` completes with full Go results.
- [x] **Add a Skylos project config** (SKY policy row #143) — DONE: `[tool.skylos]` in `pyproject.toml` with `ignore` (SKY-D215/D216/D212/G260/D222/D253/S101/D252, each dated/documented), `whitelist.names = ["app","THEMEKEY"]` + `[tool.skylos.overrides."web/app.js"] whitelist = ["*"]` for the Alpine JS cascade, and a `gate` section (`--gate` exits 0: danger 0, quality 134 baseline, deps 43 baseline, dead-code 0, secrets 0).
- [x] **Add a pre-commit policy file** (SKY row #144) — DONE: `.pre-commit-config.yaml` (local hook, `language: system` since the official hook is Python-only for a Go/JS repo). Note: `pre-commit` itself not installed on this machine yet — `pipx install pre-commit && pre-commit install` when CI/hooks are wanted.
- [x] Optional: `pip install skylos[fast]` — DONE (no-op: 4.23.0 has no `[fast]` extra; doctor's "not installed" line is a generic suggestion, checked against `pyproject.toml` at tag v4.23.0 — benign).

### Triaged findings — false positives, do NOT "fix" blindly
- [x] **Dead code ×3 in web/app.js — false positives — resolved via config (NOT deletion):**
  - `fmtRate` (app.js:22) IS referenced at app.js:1081.
  - `padLeft` (app.js:39) IS referenced at app.js:1093.
  - `app` (app.js:44) is the Alpine entrypoint (`x-data="app()"` in index.html).
  - Root cause: the JS analyzer can't see the `x-data="app()"` attribute, so `app` looks unreferenced and its module-scope helpers read as transitively dead. Resolved with `whitelist.names=["app","THEMEKEY"]` + `[tool.skylos.overrides."web/app.js"] whitelist = ["*"]` → `unused_functions` 88 → **0**. (The `# skylos: ignore` comment marker is Python-source-only; `#` is invalid JS.)
- [x] **Timing-unsafe comparison SKY-D253 ×4 (app.js:597, 634, 660, 1097) — false positives — suppressed** via `ignore`. Every hit is `e.infohash === this.rescanning` (display-state match, not a secret). Real auth is already constant-time on the server (`subtle.ConstantTimeCompare` in `tokenMatches`). `===` here is fine.
- [x] **Dependency hallucination SKY-D222 (go.mod:16 `RoaringBitmap/roaring v1.2.3`) — false positive — suppressed** via `ignore`. Package is real and is an indirect dep of anacrolix/torrent (builds fine); confirmed with `go mod verify` / `go build`.

### Real work (needs the Go engine installed first)
- [x] **Re-run `skylos . -a` after installing `skylos-go`** — DONE via `skylos . -a --json` (results in "/tmp/opencode/skylos-mutiny.json" and triaged below).
- [x] **Triage the fresh scan (engine installed) — 2026-09-13 results:** (final state after all cleanups)
  - `unused_functions` 88 → **0**: 87× `app.js` JS cascade resolved via per-file `whitelist = ["*"]` override; 1× `getInterfaceIP` (tui.go) ✓ DONE removed (zero callers, orphaned `net` import cleaned up).
  - `unused_variables` 2 → **0**: `compactLogo` (logo.go:20) ✓ DONE removed (unreferenced const — not the same as `AsciiLogo` at tui.go:1220) + `THEMEKEY` (app.js) suppressed via `whitelist.names`.
  - `unused_files` 1 = `web/app.js` SKY-E003 — benign false pos (loaded via `<script src>`, not a module tree import).
  - **Danger 80 → 0:** `SKY-D212` ×3 command-injection @ sandbox.go:125 / scanner.go:328 / tui.go:569 — false positives (all use `exec.Command(binary, args...)`; Go argv array, no shell interpolation). `SKY-G260` ×6 unclosed resources — false positives (all closed; `tty.Close()` at tui.go:597/604, `main.go:510` intentionally long-lived). `SKY-D215` ×56 path-traversal + `SKY-D216` ×7 SSRF — by-design for a torrent client (user-supplied URLs/filesystem paths; structural allowlists already in place). `SKY-S101` ×2 hardcoded-secret @ main.go:172/645 — verified false positive (config *field name* `MBAPIKey`; actual key loaded from config, no literal). `SKY-D253` ×4 + `SKY-D222` ×1 — suppressed (see above). `SKY-D252` insecure cookie @ server.go:233 — added to `ignore` per user decision (deferred until TLS lands; cookie already has HttpOnly + SameSite=Strict).
  - **Dependency vulns 64 → 43:** `pion/dtls/v3` bumped 3.0.3 → 3.1.4 (fixes GO-2026-6165); `pion/stun/v3` 3.0.0 → 3.1.5 (fixes GO-2026-6163); `golang.org/x/crypto` 0.28.0 → 0.48.0; `x/net` 0.29.0 → 0.49.0; `x/sys` 0.37.0 → 0.41.0; `x/text` 0.19.0 → 0.34.0. The remaining 43 are module-level advisories (x/crypto:30, x/net:9, otel:2, x/sys:1, x/text:1) **all confirmed non-reachable** by `govulncheck ./...` (0 reachable, was 2 reachable pre-bump). Anacrolix/torrent held at v1.58.1 — v1.61.0 causes `TestPruneUnselectedFiles` to fail (empirically confirmed, anacrolix v1.61.0 changes file lifecycle on `AddTorrentFile`/selection prune; the test's manual disk setup no longer survives the new behavior). The 43 residual are left for upstream to publish fixed versions; the gate max reflects this documented baseline.
  - **Quality 137 → 134:** `await`-in-loop `SKY-Q402` @ app.js:914 `clearPanic` → **DONE** (rewrote to `Promise.all(stalled.map(...))`). `SKY-R103` repo policy + `SKY-R104` pre-commit → DONE (see above). Remaining 134 findings — gate baseline stays 134. Decision 2026-09-13: the five big monoliths (`app` app.js:44, `Update` tui.go:655, `main` main.go:471, `loadConfigFile` main.go:201, `renderTorrentCard` tui.go:1679) are **dropped from the backlog** — no defect/security/gate issue, and they're the TUI/JS interaction core with minimal test coverage; they get refactored only when real work needs to pass through them. Tracked instead: `onKey` (app.js:233, cyc 77), `evaluateBehavior` (sandbox.go:521, 47), `runTUI` (tui.go:501, 10 params), `NewServer` (server.go:59, 7 params), `TestBrowseTorrentFile` (browse_test.go:16, 21).

### Remaining scan items — done / backlog
- [x] **SKY-D252 insecure cookie @ server.go:233 — SKIPPED (per user decision, 2026-09-13).** Already has `HttpOnly: true` + `SameSite: Strict`; `Secure: true` would silently break plain-HTTP remote auth. Added to `[tool.skylos] ignore` with a dated note; revisit when Mutiny serves TLS.
- [x] **Dependency vulns / SCA — DONE (2026-09-13).** Bumped pion/dtls/stun + x/crypto/net/sys/text as above. The comprehensive bump of anacrolix to v1.61.0 was reverted because it introduces a behavioral regression visible in `TestPruneUnselectedFiles` (the library now deletes placeholder files during selection prune — different lifecycle). Remaining 43 advisories all non-reachable per govulncheck; gate baseline set to 43 with dated note. Upstream tracking: check `go list -m -versions github.com/anacrolix/torrent` periodically.
- [x] **SKY-S101 hardcoded secrets ×2 @ main.go:172/645 — DONE: verified false positive (config FIELD name `MBAPIKey`); suppressed via `ignore`.**
- [x] **False-positive suppression — DONE** (see "Triaged findings" above; all implemented via `[tool.skylos] ignore` + per-file overrides in `pyproject.toml`).
- [x] **SKY-R103 + SKY-R104 repo-policy — DONE:** `pyproject.toml` `[tool.skylos.gate]` + `[tool.skylos.overrides]` sections added; `.pre-commit-config.yaml` written (`language: system` local hook, requires `skylos` on PATH + `pipx install pre-commit && pre-commit install`).
- [x] **Optional: `pip install skylos[fast]` — DONE** (no-op at 4.23.0; `[fast]` extra does not exist in this tag; doctor suggestion is generic).
- [ ] **Quality refactors (131 residual, gate baseline):** the five big monoliths were removed from the backlog by decision (see "Real work" above). The safe trio is DONE (reduced the count 134 → 131 by replacing `NewServer`'s 7 params with `ServerOptions` and dropping the dead `hub` arg — called with the `WebSocket` hub already bound elsewhere; `runTUI`'s 10 params with `tuiOptions`; `TestBrowseTorrentFile`'s complexity-21 body split into 4 `t.Run` subtests + extracted `seedBrowseRoot` — the split is flat and the wrapper's new C304 '78 lines' finding is cosmetic, tests compile-gated). `onKey` (was app.js:233, complexity 77) → DONE: extracted into a thin dispatcher + `onAddKey` / `onSettingsKey` / `onPickerKey` / `onFilesKey` / `onMainKey` handlers (Alpine `this` methods). `evaluateBehavior` (was sandbox.go:521, complexity 47) → DONE: extracted `isShellBinary` / `extractSignals` (struct) / `computeScore` / `behaviorVerdict` helpers; main function now wires them together. All Go tests pass, JS syntax valid.

## Packaging / release (DO when project otherwise finished)
- [x] **Version injection** — DONE: `var version = "dev"` in main.go, `--version` flag prints and exits, `Version` field added to `ServerOptions`/`Server`/`api.ServerOptions`, surfaced in `GET /api/status`. GoReleaser injects real version via `-X main.version={{.Version}}` ldflags.
- [x] **GoReleaser** (`.goreleaser.yaml`) — DONE: builds `linux/amd64`, `linux/arm64`, `windows/amd64` (CGO disabled); `.tar.gz` archives (linux) + `.zip` (windows); SHA256 checksums + SPDX SBOM; cosign signing; GitHub Releases publish on `v*` tag; AUR PKGBUILD + winget manifest + chocolatey package auto-generated.
- [x] **GitHub Actions** (`.github/workflows/release.yml`) — DONE: triggers on `v*` tag push, sets up Go 1.24 + GoReleaser, runs `goreleaser release --clean` with `GITHUB_TOKEN` + `AUR_PRIVATE_KEY` secrets.
- [x] **One-line installer** — DONE: `build/install.sh` split into `--release [VERSION]` (download latest/specific GitHub release tarball, verify sha256, install binary + icons + desktop entries + systemd service) and `--source` (current `go build` fallback). Uses `@BINDIR@` placeholder substitution. Dev-friendly URL:
  `curl -fsSL https://raw.githubusercontent.com/Lumb3rH4ck/mutiny/main/install.sh | sh`
- [x] **AUR PKGBUILD `mutiny-bin`** — DONE: auto-generated by GoReleaser `aurs` section on release. Pulls linux tarball, installs to `/usr/bin`, depends: `clamav`, optdepends: `docker` + `yara`, ships icons + desktop entries + systemd user service.
- [x] **Fix desktop entries** — DONE: `build/mutiny.desktop` + `build/mutiny-tui.desktop` now use `@BINDIR@/mutiny` placeholder; install.sh substitutes at install time.
- [x] **Fix `build/mutiny.service`** — DONE: dropped `ProtectHome=read-only`, scoped `ReadWritePaths=%h/Downloads/Mutiny %h/.config/mutiny`, uses `@BINDIR@` placeholder, installs to `~/.config/systemd/user/`.
- [x] **Omarchy shell widget `mutiny.status`** — DONE: Quickshell `bar-widget` plugin at `~/.config/omarchy/plugins/mutiny.status/` (`manifest.json` + `MutinyStatus.qml`). Polls `http://127.0.0.1:3030/api/torrents` every 5s for active/downloading/threat counts. Shows anchor icon (⚓) normally, bolt (⚡) when threats detected. Click opens web UI. Added to bar right section in `~/.config/omarchy/shell.json`. Visible only when Mutiny is running and has activity.

## Release secrets (required for CI/CD)

- [ ] **Set `AUR_PRIVATE_KEY` in GitHub repo** — `Settings → Secrets and variables → Actions → New repository secret`. Generate with `ssh-keygen -t ed25519 -C "aur@mutiny" -f ~/.ssh/aur_mutiny -N ""`. Add the **private** key as the secret. Add the **public** key (`~/.ssh/aur_mutiny.pub`) as an SSH key on the AUR account at https://aur.archlinux.org/account/Lumb3rH4ck/settings/
- [ ] **First-time winget approval** — first release auto-PRs to `microsoft/winget-pkgs`, needs manual review by Microsoft maintainers
- [ ] **First-time chocolatey approval** — first push to chocolatey.org needs manual moderator approval

See [Mutiny.md](Mutiny.md) for the full release & packaging guide.

## Windows platform support
- [x] **bind_windows.go** — DONE: no-op `vpnBindDialer` (no SO_BINDTODEVICE on Windows); `vpnBindResolver` + `vpnListenHostAddr` shared (use `net.InterfaceByName`, works on both).
- [x] **scanner process management** — DONE: extracted `procAttr()` + `killProcessTree()` into `proc_linux.go` (`Setpgid` + `Kill(-pid, SIGKILL)`) and `proc_windows.go` (`CREATE_NEW_PROCESS_GROUP` + `taskkill /F /T /PID`).
- [x] **Sandbox Linux-only** — DONE: `sandbox.go` + `sandbox_test.go` now have `//go:build linux`; `sandbox_windows.go` provides stubs (`New` returns nil, `Analyze` returns "not supported", `IsExecutable` returns false). Types moved to `sandbox_common.go`.
- [x] **Other shims** — DONE: `setUmask()` (no-op on Windows), `openPathPlatform()` (`cmd /c start` on Windows), `clamDBDirOwnerPlatform()` (returns "0:0" on Windows), `cmd/sbx` marked Linux-only.
- [x] **Build verified** — `GOOS=windows GOARCH=amd64 go build ./...` passes; Linux build + all tests still green.